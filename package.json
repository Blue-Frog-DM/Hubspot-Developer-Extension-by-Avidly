{
  "name": "hubspot-developer-extension-by-avidly-development",
  "version": "1.0.0",
  "description": "A Chrome extension to supercharge HubSpot development, maintained by Avidly Development.",
  "author": "Avidly Development",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf dist",
    "copy": "mkdir -p dist && cp -r src/* dist/",
    "build": "npm run clean && npm run copy",
    "lint": "eslint src",
    "lint:fix": "eslint src --fix"
  },
  "devDependencies": {
    "@eslint/css": "^0.11.0",
    "@eslint/js": "^9.34.0",
    "@eslint/json": "^0.13.2",
    "@eslint/markdown": "^7.2.0",
    "eslint": "^9.34.0",
    "globals": "^16.3.0"
  }
}
